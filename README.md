# AutoLedger

This repository was empty — I've scaffolded a minimal Node.js Express app so you can run it locally.

Run locally:

```bash
cd /workspaces/AutoLedger
npm install
npm start
```

Then open http://localhost:3000 or check the health endpoint http://localhost:3000/health
# AutoLedger